---
name: Smart Tesla
description: 차내 태블릿에 상시 켜져 있는 정비 매뉴얼 분해도 — 제도지에 단색 잉크, 유채색은 정정 2색뿐
colors:
  paper: "#F2F0E9"
  paper-title: "#EAE7DE"
  paper-fill: "#E1DDD1"
  rule-hairline: "#B9B5A8"
  ink: "#1A1A17"
  ink-pressed: "#44433D"
  ink-muted: "#5D5C55"
  ink-faint: "#6E6C64"
  draft-blue: "#1F5C8C"
  draft-red: "#C8321E"
  heat-red: "#B3411F"
  caution-ochre: "#A1601A"
  caution-ochre-text: "#7E4712"
  caution-ochre-faint: "#EDE4D2"
  blueprint: "#101619"
  blueprint-title: "#161E22"
  blueprint-fill: "#1E282D"
  blueprint-rule: "#32414A"
  blueprint-ink: "#E7ECEE"
  blueprint-ink-pressed: "#B8C3C8"
  blueprint-ink-muted: "#9DACB3"
  blueprint-ink-faint: "#8B9AA2"
  blueprint-blue: "#6FB6E0"
  blueprint-red: "#E8624E"
  blueprint-heat: "#E08A5A"
  blueprint-caution: "#D9A441"
  blueprint-caution-faint: "#2B2718"
typography:
  dimension:
    fontFamily: "Monospace (Noto Sans Mono CJK / 시스템 고정폭)"
    fontSize: "84sp (Expanded) · 68sp (Medium) · 56sp (Compact) 상한, 실제 크기는 칸 폭에서 실측해 정한다"
    fontWeight: 500
    lineHeight: "100sp"
    letterSpacing: "-0.035em"
    fontFeature: "tnum"
  callout-value:
    fontFamily: "Monospace"
    fontSize: "20sp"
    fontWeight: 500
    lineHeight: "26sp"
    letterSpacing: "-0.2sp"
    fontFeature: "tnum"
  metric:
    fontFamily: "Monospace"
    fontSize: "20sp"
    fontWeight: 500
    lineHeight: "26sp"
    letterSpacing: "-0.2sp"
    fontFeature: "tnum"
  callout-number:
    fontFamily: "Monospace"
    fontSize: "11sp"
    fontWeight: 500
    lineHeight: "14sp"
    letterSpacing: "0.6sp"
    fontFeature: "tnum"
  sheet-title:
    fontFamily: "Sans (Noto Sans CJK / 시스템 기본)"
    fontSize: "17sp"
    fontWeight: 600
    lineHeight: "24sp"
    letterSpacing: "0.4sp"
  section-title:
    fontFamily: "Sans"
    fontSize: "14sp"
    fontWeight: 600
    lineHeight: "20sp"
    letterSpacing: "0.7sp"
  row-title:
    fontFamily: "Sans"
    fontSize: "13sp"
    fontWeight: 600
    lineHeight: "19sp"
    letterSpacing: "0.5sp"
  body:
    fontFamily: "Sans"
    fontSize: "14sp"
    fontWeight: 400
    lineHeight: "21sp"
    letterSpacing: "0.1sp"
  note:
    fontFamily: "Sans"
    fontSize: "12sp"
    fontWeight: 400
    lineHeight: "18sp"
    letterSpacing: "0.1sp"
  label:
    fontFamily: "Sans"
    fontSize: "14sp"
    fontWeight: 600
    lineHeight: "18sp"
    letterSpacing: "0.6sp"
  label-small:
    fontFamily: "Sans"
    fontSize: "11sp"
    fontWeight: 500
    lineHeight: "16sp"
    letterSpacing: "0.6sp"
rounded:
  square: "0dp"
  pill: "999dp"
spacing:
  xs: "4dp"
  sm: "8dp"
  md: "16dp"
  lg: "24dp"
  xl: "32dp"
  xxl: "48dp"
components:
  button-primary:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.paper-title}"
    typography: "{typography.label}"
    rounded: "{rounded.square}"
    padding: "8dp 16dp"
    height: "52dp"
  button-primary-pressed:
    backgroundColor: "{colors.ink-pressed}"
  button-secondary:
    backgroundColor: "{colors.paper-fill}"
    textColor: "{colors.ink}"
    rounded: "{rounded.square}"
    padding: "8dp 16dp"
    height: "52dp"
  button-ghost:
    backgroundColor: "transparent"
    textColor: "{colors.ink-muted}"
    rounded: "{rounded.square}"
  button-danger:
    backgroundColor: "{colors.paper-fill}"
    textColor: "{colors.draft-red}"
    rounded: "{rounded.square}"
  button-small:
    typography: "{typography.label-small}"
    padding: "0dp 12dp"
    height: "48dp"
  region-ruled:
    backgroundColor: "{colors.paper}"
    rounded: "{rounded.square}"
    padding: "16dp 0dp 24dp 0dp"
  title-block:
    backgroundColor: "{colors.paper-title}"
    textColor: "{colors.ink-muted}"
    typography: "{typography.row-title}"
    padding: "8dp 16dp"
  callout-number:
    backgroundColor: "transparent"
    textColor: "{colors.ink}"
    typography: "{typography.callout-number}"
    rounded: "{rounded.pill}"
    size: "19dp"
  callout-number-highlighted:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.paper}"
  draft-toggle:
    backgroundColor: "{colors.ink}"
    textColor: "{colors.ink}"
    typography: "{typography.label-small}"
    rounded: "{rounded.square}"
    size: "16dp"
    height: "48dp"
  table-header:
    textColor: "{colors.ink-faint}"
    typography: "{typography.label-small}"
    padding: "0dp 0dp 4dp 0dp"
  nav-sheet-tab:
    backgroundColor: "{colors.paper-title}"
    textColor: "{colors.ink-faint}"
    typography: "{typography.row-title}"
    rounded: "{rounded.square}"
    padding: "8dp 16dp"
    width: "112dp"
    height: "48dp"
  nav-sheet-tab-active:
    textColor: "{colors.ink}"
---

# Design System: Smart Tesla

## Overview

**Creative North Star: "정비 매뉴얼 분해도(The Service Manual Exploded View)"**

이 앱은 차 안에 상시 거치된 태블릿에서 하루 종일 켜져 있고, 그 시간의 대부분은 아무도 만지지 않는다.
매크로가 알아서 하기 때문이다. 그래서 화면의 목표는 "빨리 누르기"가 아니라 **"안 만지고 알아채기"** 다.
정비 매뉴얼의 분해도를 세계로 고른 이유가 그것이다 — 도면은 정상일 때 아무 표시도 하지 않는다.
이상이 있는 자리에만 잉크가 든다. "정상은 조용해야 한다"가 절제의 취향이 아니라 세계의 구조 자체가 된다.

판은 제도지다. 미색이 살짝 든 종이(`paper`)에 단색 잉크(`ink`)로 인쇄됐고, 밤에는 같은 도면을
청사진 네거티브(`blueprint`)로 다시 인쇄한다 — 흄내기가 아니라 실제로 있던 인쇄법이라 낮과 밤이
다른 앱으로 갈라지지 않는다. 카드가 없다. 판은 종이와 같은 색이고, 층은 오직 **괘선**으로만 생긴다.
모서리는 전부 0dp다. 선 굵기는 0.5·1·2dp 3계층으로만 갈리고, 이 계층이 색을 대신해 정보의 무게를 만든다.

시트 1(제어)의 주인공은 목록이 아니라 **Model Y 평면 선도**다. 실물 비례 2.47:1로 앉고, 상단 3개·
하단 2개의 지시선이 부위에서 라벨로 뻗는다. 문이 열리면 그 문이 경첩을 축으로 45도 벌어져 적색으로
그려진다 — "운전석 도어 열림"이라는 글자를 읽고 어느 문인지 다시 생각하는 단계가 없다.
위치가 라벨이고, 부위 하나가 곧 값이고 곧 조작이다.

이 세계는 **이전 세계를 대체했다.** 0.8.31에서 사용자가 직접 고른 "각지고 조용한 판"이 그것이다 —
무채색 회색, 4dp 모서리, 1dp 테두리 카드, 검정 액센트, Material Icons Rounded, Material Switch,
그리고 큰 숫자 하나에 라벨을 붙인 히어로 타일. 그 전부가 지금은 **안티레퍼런스**다. 되살리지 않는다.

**Key Characteristics:**
- 제도지 + 단색 잉크. 밤엔 같은 도면의 청사진 네거티브
- 카드 없음 · 그림자 없음 · 모서리 0dp — 층은 괘선으로만
- 선 굵기 3계층(0.5 / 1 / 2dp)이 색 대신 계층을 만든다
- 유채색은 도면 정정 2색(적·청)뿐. 정상은 완전히 무채색
- 위계가 거꾸로 — 가장 큰 글자는 제목이 아니라 기입된 치수(실내 온도)
- 계측값은 전부 고정폭. 자릿수가 바뀌어도 화면이 흔들리지 않는다
- 상태는 색보다 먼저 **무늬**로 말한다 — 윤곽선 / 해칭 / 채움
- 아이콘은 자체 제작 도면 기호. Material 아이콘·스위치·이모지 없음

## Colors

제도지 한 장과 잉크 한 병으로 화면 전체를 그리고, 유채색은 도면 정정 관행의 적·청 두 개만 남겼다.

### Primary
- **잉크(#1A1A17)**: 글자·부품 윤곽·굵은 괘선·채워진 토글·주 버튼 면. 누를 수 있다는 표시도 유채색이 아니라
  **잉크가 진해지는 것**으로 한다(`electric` = `ink`). 누른 동안은 **눌린 잉크(#44433D)** 로 내려간다.
- **제도지(#F2F0E9)**: 화면 바탕이자 판 바탕. 판(`graphite`)이 바탕과 같은 색이라 카드가 떠 있지 않다.
  적색 면 위에 얹는 글자색(`onDanger`)도 이 색이다.

### Secondary — 도면 정정 2색
- **제도 청(#1F5C8C)**: 차가 **식히는 중**. 캐빈·시트 해칭, 냉방 중인 기입 치수, 시트 통풍 단계.
- **정정 적(#C8321E)**: **지금 사람이 봐야 할 것**. 열린 문의 벌어진 판, 채워진 부위, 경보 라벨의 적색 면,
  실패 배너의 두 괘선, 삭제 문구.
- **가열 적(#B3411F)**: 차가 **데우는 중**. 정정 적과 같은 계열의 농담으로 갈라 "작동 중"과 "봐야 할 것"을
  구별한다 — 채도가 낮아 곁눈에 덜 걸린다.

### Tertiary — 주의 황토
- **주의 황토(#A1601A)** / **주의 글자(#7E4712)** / **주의 옅은 면(#EDE4D2)**: 시뮬레이터 연결처럼
  "틀린 건 아니지만 평상시는 아닌" 상태에만. 밝은 황토는 옅은 면 위에서 안 읽혀 글자만 진한 값으로 분리한다.

### Neutral
- **표제란 종이(#EAE7DE)**: 표제란·시트 목차·개정란·상세도 바탕. 종이보다 한 톤 눌린 면.
- **채워진 면(#E1DDD1)**: 보조 버튼 면, 세그먼트 트랙, 스텝 버튼. 눌린 상태의 면.
- **치수선(#B9B5A8)**: 괘선·격자·지시선·표 경계. 정보를 지지 않는 보조선의 색.
- **물러선 잉크(#5D5C55)**: 표제란 값, 부품 참조선, 정상 상태 문자열. `ok` 계열이 이 색이다 —
  **도면에서 "이상 없음"은 표시가 없다는 뜻**이라 정상에 초록을 주지 않는다.
- **가장 얇은 잉크(#6E6C64)**: 부품 라벨·표 머리글·치수 이름. 11sp로 직사광에서 읽어야 하는 글자라
  4.5:1을 넘기도록 다시 잡았다(제도지 위 4.68:1).

### 밤 — 청사진 네거티브
같은 도면을 다른 인쇄법으로 낸 것이다. 바탕 **#101619**, 표제란 **#161E22**, 선 **#E7ECEE**.
순검정을 쓰지 않는다 — 값싼 패널에서 잔상이 남고 야간 운전에 눈이 아프다.
정정 2색도 함께 밝아진다(청 #6FB6E0 · 적 #E8624E). 가장 얇은 잉크는 여기서도 4.5:1을 넘긴다(6.31:1).

### Named Rules
**The Two Inks Rule.** 유채색은 정정 적과 제도 청 **두 개뿐**이다. 적은 "지금 사람이 봐야 할 것",
청은 "차가 일하는 중". 그 밖의 모든 것은 무채색 잉크다. 새 의미에 새 색을 만들지 않는다.

**The One Alert Rule.** 유채색이 두 군데서 동시에 뜨면 어느 쪽을 봐야 하는지가 사라진다.
열린 문이나 실패 배너가 있으면 기입 치수는 잉크로 물러선다(`insideColor()`). 문이 열려 있으면
잠금 해제는 당연한 결과라 경보를 달지 않는다(`alert = ... && openings.isEmpty()`).

**The No Green Rule.** 정상에 색을 주지 않는다. `ok`는 초록이 아니라 물러선 잉크다.
상시 켜진 화면에서 상시 켜진 강조는 강조가 아니다.

**The Unlocked-Is-Not-An-Alarm Rule.** 잠금 해제로 차체 전체를 적색으로 칠하지 않는다.
차 옆에 서 있으면 늘 참인 평상 상태인데 화면에서 가장 큰 잉크가 되어, 정작 봐야 할 경보를 이길 수 없게 만들었다.
잠금은 04번 배지 하나로 말한다.

## Typography

**Display / Body Font:** 시스템 기본 산세리프(`FontFamily.Default` → 한국어 환경에서 Noto Sans CJK)
**Measurement Font:** 시스템 고정폭(`FontFamily.Monospace`) — 숫자·단위·부품번호·시각 전용

**Character:** 전용 서체를 **의도적으로 심지 않았다.** UI가 한국어이고 이 기기에 실을 수 있는 한글 서체는
시스템 서체 하나뿐이라, 20MB를 더 실어도 렌더 결과가 똑같다(자체 업데이트로 배포되는 APK라 용량이 곧 비용이다).
그래서 목소리를 서체가 아니라 **세 가지**가 만든다 — 모든 계측 역할에 붙는 고정폭, 절제된 자간, 그리고 거꾸로 된 위계.

도면에는 큰 제목이 없다. 제일 큰 글자는 **기입된 치수**이고 도면 이름은 표제란 안에 작게 적힌다.
그래서 이 앱의 위계도 거꾸로다: 실내 온도가 84sp, 시트 제목은 17sp, 부품 라벨은 11sp다.

### Hierarchy
- **Dimension** (Mono, W500, 84/68/56sp, tracking −0.035em): 실내 온도. 화면에서 유일하게 압도적으로 큰 것.
  자간을 `em`으로 잡는다 — `sp`로 두면 크기를 줄여도 절대값이 남아 비율이 자간 하한(−0.04em)을 넘었다.
- **Callout value / Metric** (Mono, W500, 20sp): 지시선 끝의 값, 치수표의 값, `°C` 단위.
  고정폭이라 22.5→22.6에서 숫자 전체가 좌우로 움직이지 않는다.
- **Callout number** (Mono, W500, 11sp, +0.6sp): 지시선 끝 원 안의 두 자리 번호. 도면과 표를 잇는 유일한 끈.
- **Sheet title** (Sans, W600, 17sp, +0.4sp): 표제란 글자. 크지 않다.
- **Section title** (Sans, W600, 14sp, +0.7sp): 구역 이름. 넓은 자간으로 눕는다.
- **Row title** (Sans, W600, 13sp, +0.5sp): 표 한 행의 이름, 표제란 값.
- **Body / Note** (Sans, W400, 14 / 12sp): 주기(註記). 도면의 설명 글은 작다.
- **Label** (Sans, W600, 14 / 12sp, +0.6 / +0.5sp): 버튼 라벨. 자간을 벌려 명판처럼 읽힌다.
- **Label small** (Sans, W500, 11sp, +0.6sp): 부품 라벨·표 머리글·치수 이름. 가장 물러선 글자.

### Named Rules
**The Inverted Hierarchy Rule.** 가장 큰 글자는 제목이 아니라 값이다. 화면 제목을 두지 않는다 —
어느 시트인지는 좌측 목차가 이미 말한다.

**The Monospace-Is-Measurement Rule.** 고정폭은 멋내기가 아니라 계측이다. 숫자·단위·부품번호·시각에만
쓰고, 한글이 섞이는 라벨과 본문에는 쓰지 않는다. 모든 역할에 `tnum`이 켜져 있다.

**The Korean Tracking Ceiling Rule.** 자간을 1.6sp(0.145em)까지 벌렸더니 한글 자모 덩어리가 흩어졌다.
라틴 소형 대문자 관례를 한글에 그대로 쓸 수 없다 — 0.055em까지만 벌린다.

## Layout

실기기는 **ALLDOCUBE iPlay 60 mini Pro**, 8.4" 1920×1200 ≈ **960×600dp 가로 한 장**이다. 세로가 짧다.
폭은 루트에서 한 번 재서 `LocalPane`으로 내려보낸다 — Compact(<600dp) / Medium(<900dp) / Expanded(≥900dp).
실기기는 항상 Expanded다.

간격은 **8dp 그리드** 하나만 쓴다(4·8·16·24·32·48). 화면에서 raw dp를 쓰지 않는다.

**시트 1(제어)** — 좌측 **68%** 가 작도 영역, 우측 32%가 치수 기입란이고, 그 둘 사이는 여백이 아니라
1dp 세로 괘선으로 가른다. 하단은 표제란 한 줄(도면번호 · 개정 · 차량 · 갱신 · 자동화 · 연결).
**스크롤이 없다** — 차에 고정된 화면에서 스크롤은 못 본 정보를 만든다.
지시선 라벨은 `weight(1f)` 균등 분할 칸에 놓고, 지시선의 시작점을 같은 공식(`columnCenterX`)으로 계산한다.
측정 후 배치를 하면 두 번 그려야 하고 스냅샷이 흔들린다.

**시트 2(매크로)** — 절차 명세표. 열 비율은 상수 하나(`0.06 / 0.44 / 0.10 / 0.20 / 0.20`)를
머리글과 본문이 나눠 갖는다. 어긋나면 표가 아니라 두 개의 줄이 된다. 하단은 개정란(실행 기록 3줄).

**시트 3(설정)** — 서로 무관한 묶음이라 여기만 2단이다. 본문 2단은 시트 1·2에서는 금지다.
소분류가 10개까지 늘면서 좌/우의 "자주 만지는 것 / 어쩌다 보는 것" 배분이 무너졌고
(오른쪽에 업데이트·절전·길안내·백업·차량·음성·진단이 몰렸다), 한 장에 다 세우니
스크롤로만 찾게 됐다. 그래서 시트 안에 **중분류 목차 한 줄**(48dp 세그먼트, 네 칸:
주행·자동화·차량·기기)을 두고 고른 칸만 편다. 목차와 내용은 그림자가 아니라
`Stroke.bold` 괘선 한 줄로 가른다. 칸마다 좌우 둘 다 채워지도록 배치한다 —
한쪽이 비면 도면이 미완성으로 보인다(폴링 주기를 차량 칸으로 옮긴 이유).
화면 제목은 여전히 없다. 목차는 제목이 아니라 시트 안의 색인이다.

**좁아질 때** — Compact에서는 선도를 **접고 값만 남긴다.** 둘을 함께 줄이면 둘 다 못 읽는다.
접힌 화면에서는 지시선 라벨이 부품 명세표 한 줄씩으로 다시 선다(번호 · 이름 · 값).
치수 기입란은 넓을 때 아래(표제란 쪽)로 붙고, 좁을 때 위에서 시작한다 — 가운데 정렬이면
값이 아직 없을 때 화면에서 가장 큰 빈 공간이 우상단에 생긴다.

**시트 이동** — 폭이 아니라 **방향**으로 가른다. 가로면 좌측 112dp 레일, 세로면 하단 탭.
차내 태블릿은 세로로 세워도 폭이 600dp를 넘어 레일로 잡혔고, 시트가 3장뿐이라 세로에선 레일이 본문 폭만 먹었다.

**터치 타깃은 48dp** 가 기준이다(안드로이드 최소치이고, 사용 장면이 장갑 낀 손과 흔들리는 차다).
버튼·토글·시트 탭·지시선 라벨·표 행·상세도 닫기·숫자 스테퍼·단계 세그먼트·배너 닫기가 **전부 48dp**다.
44dp는 코드에 한 곳도 남아 있지 않다(`grep '44\.dp'` 결과 0). 이 앱은 안드로이드 최소치를 하한이 아니라
기준으로 쓴다 — 장갑 낀 손과 흔들리는 차가 사용 장면이라 더 작아질 이유가 없다.

**글자 배율** — 시스템 1.3배에서 잘리지 않아야 한다. 원번호의 지름과 지시선 라벨 줄 높이는 `fontScale`에
비례해 함께 커진다.

기입 치수는 상수를 쓰지 않는다. **`TextMeasurer`로 이 글자를 이 서체로 재서 칸 폭에 맞춘다** —
`min(상한, 칸에 들어가는 크기)`. 여기까지 오는 데 두 번 틀렸고, 둘 다 남겨 둘 만한 교훈이다:

1. 배율 상한을 `1.3`으로 박았다. 찍은 숫자였고, 실측하니 네 글자가 1.3배에서 칸을 18dp 넘겼다.
2. 고정폭 advance를 `0.6em`으로 가정해 계산했다. 이 기기의 실제 고정폭은 훨씬 좁아
   넘치지는 않았지만 배율을 올릴 때 값이 **작아졌다** — 접근성 의도가 거꾸로 됐다.

3. `TextMeasurer`가 글자 배율을 반영하지 **않는다**고 판단해 잰 폭에 배율을 곱했다. 틀렸다 —
   측정기는 `LocalDensity`를 통해 재고 거기에 배율이 들어 있다. 같은 것을 두 번 보정해
   값이 다시 작아졌다(실측 56px → 39px).
4. 단위(°C) 자리를 `56.dp × fontScale`로 예약했다. 그 곱이 값의 성장분을 그대로 먹어
   또 작아졌다(56px → 51px).

지금은 **아무것도 추정하지 않는다.** 값과 단위를 각각 재고, 남는 폭에 값을 맞춘다.
결과는 **칸이 허락하는 최대 크기**다 — 배율 1.0과 1.3에서 글자 높이가 64px·63px로 사실상 같고
(칸이 이미 꽉 차 있어 더 커질 자리가 없다), 다섯 글자(`-10.5`)는 51px로 스스로 줄어든다.
마지막 방어선으로 값 쪽에 `weight(1f, fill = false)`를 둬, 계산이 어긋나도 단위를 칸 밖으로 밀지 못한다.

**검증을 스크린샷에 맡기면 안 된다.** 값 바로 아래 치수선이 칸 전폭을 채우기 때문에
"오른쪽 끝"을 재면 크기가 어떻게 바뀌어도 늘 같은 값(974)이 나온다 — 실제로 이 함정에 빠져
"안 줄어든다"고 잘못 확인했다. 그래서 계산을 `fitInscribedSp`라는 순수 함수로 떼고
`InscribedSizeTest`가 "배율을 올려도 렌더 폭이 줄지 않는다"를 직접 단정한다.
`WideFontScaleTest`는 그 위에서 눈으로 보는 컷이다 — 배율 1.3에 **값이 들어간** 상태로
네 글자와 다섯 글자를 각각 찍는다. 값 없는 화면은 기입 치수를 아예 그리지 않아 아무것도 증명하지 못한다.

## Elevation & Depth

**그림자가 없다.** 하나도 없다. 이 세계에 깊이는 존재하지 않는다 — 도면은 인쇄된 평면이고,
판(`graphite`)은 종이(`void`)와 **같은 색**이다. 그래서 "떠 있는 면"이라는 개념 자체가 없다.

층은 세 가지로만 생긴다:
1. **괘선** — 구역의 위에 1dp(또는 강조 시 2dp) 선을 긋고 그 아래를 그 구역으로 삼는다.
2. **선 굵기 계층** — hair 0.5dp(치수선·격자·지시선·숨은 선), thin 1dp(부품 윤곽·판 경계·표 괘선),
   bold 2dp(외곽선·주요 부품·지금 고른 것). 실기기 패널이 값싸서 0.5dp가 사라질 수 있어,
   정보를 지고 있는 선은 최소 thin으로 올린다.
3. **한 톤 눌린 종이** — 표제란·시트 목차·개정란·상세도만 `paper-title`을 깐다. 떠오르는 것이 아니라
   인쇄 영역이 다른 것이다.

### Named Rules
**The No-Box Rule.** 상자를 그려 층을 만들지 않는다. 4면 테두리 카드 다섯 개가 쌓인 화면은
도면이 아니라 카드 목록이다. `TCard`는 상자가 아니라 **위에 괘선 하나를 긋고 그 아래를 구역으로 삼는 것**이다.

**The Rule-Not-Gap Rule.** 칸을 여백으로 나누지 않는다. 여백으로 나누면 도면이 아니라 그냥 문단이 된다.
표제란의 칸, 표의 행, 작도 영역과 기입란의 경계는 전부 선이다.

## Shapes

**모서리는 0dp다.** 버튼·판·타일·세그먼트 전부. 이전 세계의 4dp는 "각지게 하려고 했다"는 표시일 뿐이었고,
제도된 판에서는 선이 만나는 곳이 그냥 만나야 한다. 알약(999dp)은 **실제로 원인 것**에만 남긴다 —
지시선 끝의 원번호, 배지 점, 진행 선의 끝.

선 끝단은 전부 `StrokeCap.Square`다. 둥근 끝은 잉크가 아니라 붓 자국으로 보인다.

**무늬가 형태의 일부다.** 상태는 색보다 먼저 그리기 방식으로 갈린다:
- **윤곽선만** = 정상 (`PartState.Plain`)
- **해칭**(사선 무늬) = 작동 중. 청 해칭 = 식히는 중, 적 해칭 = 데우는 중
- **채움** = 지금 봐야 할 것 (`PartState.Alert`)
- **파선(숨은 선)** = 덮여 있어 보이지 않는 것 — 배터리 팩, 펜더 안의 타이어, 문이 원래 닫혀 있던 자리

**자체 제작 도면 기호.** Material 아이콘을 쓰지 않는다 — 둥근 모서리와 채워진 면이 도면 문법과 어긋난다.
`DraftMark`의 기호는 전부 24dp 격자, **1.5dp 단일 굵기**, 사각 끝단, 채움 없는 직선·직각뿐이다.
굵기를 인자로 열지 않은 것은 일부러다. 기호가 굵기별로 갈리면 도면이 아니게 된다.
취소는 대각 두 선, 추가는 십자, 삭제는 상자 안 사선, 실행은 **윤곽** 삼각, 지목은 삼각 지시자
(★ 글리프는 폰트마다 모양이 달라지고, 별은 도면 기호가 아니다).

### Named Rules
**The Zero-Radius Rule.** 새 모서리 값을 만들지 않는다. `Radius.square`(0dp) 아니면 `Radius.pill`(999dp)뿐이고,
pill은 실제로 원인 것에만 쓴다.

**The Pattern-Before-Color Rule.** 상태를 색만으로 바꾸지 않는다. 흘깃 보는 화면에서 색만 바뀐 것은
걸리지 않는다. 무늬(해칭·채움·파선)를 함께 바꾼다. 색맹 사용자에게도 같은 이유로 옳다.

## Components

### Buttons
- **Shape:** 각(0dp). 그림자·글로우 없음. 누르면 0.97배로 살짝 줄어든다(160ms).
- **Primary:** 잉크 면 + 표제란 종이색 글자, 높이 52dp, 좌우 16dp. 화면에서 유일하게 채워진 잉크 면이다.
- **Secondary:** 채워진 종이면(`paper-fill`) + 잉크 글자 + 1dp 치수선 테두리.
- **Ghost:** 면 없음 + 물러선 잉크 글자. 누르는 동안만 면이 든다.
- **Danger:** 빨간 덩어리를 만들지 않는다 — 조용한 면에 정정 적 글자 + 35% 투명도 적색 테두리.
  화면에서 적색 **면**은 "지금 봐야 할 상태" 전용이라 버튼이 그 자리를 뺏으면 안 된다.
- **Small:** 높이 48dp, 좌우 12dp, label-small. 표 도구 줄과 표제란 안의 보조 동작에만.
- **Disabled:** 면 없음 + 가장 얇은 잉크 글자 + 50% 투명도 테두리.

### Toggles
Material Switch를 쓰지 않는다. 알약 트랙과 미끄러지는 손잡이는 이 세계의 문법이 아니고,
무엇보다 **켜짐/꺼짐이 색으로만 갈린다.** 도면은 채움으로 갈린다 — **채운 16dp 사각 = 켜짐,
빈 사각 = 꺼짐**, 옆에 상태어("켬"/"끔"/"감시")를 함께 적어 색 없이도 읽힌다. 행 높이 48dp.

### Cards / Containers (`TCard` — 구역)
- **Corner Style:** 없음(0dp). 상자가 아니다.
- **Background:** 종이색 그대로. 판이 바탕과 같은 색이다.
- **Shadow Strategy:** 없음. Elevation & Depth 참조.
- **Border:** 위에 괘선 하나뿐 — 기본 1dp 치수선색, `outlined = true`면 2dp 잉크(실행 중·강조 구역).
- **Internal Padding:** 위 16dp, 아래 24dp. 좌우 패딩은 두지 않는다(상위 화면의 24dp를 따른다).

**`Modifier.draftBlock(tone)`** — 카드보다 가벼운 구획. 위에 2dp 괘선 하나와 위쪽 패딩만 준다.
안내·경고처럼 글 묶음 하나를 떼어 놓을 때 쓰고, 경고는 `tone = T.Warn`으로 괘선에 색을 넣는다.
등록 화면의 안내 패널 세 개가 채운 면에서 이것으로 바뀌었다 — 채움이 쌓이면 판이 카드 목록이 된다.

### Inputs / Fields
- **Number stepper:** 차 안이라 키보드 입력을 피한다. `−` / `+` 두 칸(48dp, 채워진 종이면)과
  가운데 값(최소 폭 76dp — 고정 폭이면 "3600초"나 글자 확대에서 잘린다). 글리프 대신 벡터 기호를 쓴다.
- **Level segment:** 슬라이더 대신 4칸 세그먼트(높이 48dp). 채워진 종이면 트랙 위에 선택 칸만 도드라진다.
  선택 칸의 면은 그 모드의 의미색(청=통풍, 적=열선)이고, OFF 칸만 흰 면 + 1dp 테두리
  (흰 면 위 흰 면이라 테두리가 없으면 선택 여부가 안 보인다). 열선 적색 면 위 글자는 대비 미달이라 잉크로 뒤집는다.
- **Text field (`DraftField`):** Material의 `OutlinedTextField`를 쓰지 않는다. 네 면을 감싼 둥근 테두리에
  라벨이 홈을 파고 떠 있는 그 모양은 안드로이드 기본 부품의 서명이라, 다른 걸 다 바꿔도 그 칸 하나가
  "머티리얼 앱"이라고 말한다. 도면의 기입란은 **라벨이 위에 적히고 값은 밑줄 위에 앉는다.**
  초점이 오면 밑줄이 1dp→2dp로 굵어진다(색을 쓰지 않는다). 오류면 밑줄과 라벨이 정정 적이 된다.
  단위는 `suffix`로 밑줄 오른쪽 끝에, 주기는 `note`로 밑줄 아래에 붙는다. 앱의 모든 입력칸이 이것이다.

### Navigation
아이콘을 쓰지 않는다. 도면집은 아이콘이 아니라 **시트 번호**로 넘긴다 — "1 / 3" 위에 이름이 붙는다.
번호가 있으면 몇 장 중 몇 번째인지도 함께 알 수 있어 아이콘 세 개보다 정보가 많다.
바탕은 표제란 종이색이고, 지금 펼친 시트는 **색이 아니라 2dp 잉크 선**으로 표시한다 — 레일이면 왼쪽에,
하단 탭이면 위쪽에 선다. 시트 사이는 표제란과 같은 방식으로 1dp 괘선으로 나눈다. 레일 폭 112dp.

### Title Block (표제란)
도면에서 "이게 무슨 도면이고 언제 것인가"는 큰 제목이 아니라 시트 하단의 표제란에 작게 적힌다.
이 앱도 그렇게 한다 — 차 이름과 연결 상태가 화면 상단을 먹지 않고 아래 한 줄에 눕는다.
칸은 `라벨(11sp, 가장 얇은 잉크) + 값(13sp, 물러선 잉크)` 쌍이고, 칸 사이는 18dp 높이의 세로 괘선이다.
줄 접기는 **호출부가 `perRow`로 명시한다**(좁은 화면은 2칸, 넓은 화면은 한 줄). 한때 `FlowRow`로
흘려 보냈는데, 어디서 접힐지 모르니 새 줄이 괘선으로 시작하거나 앞 줄 끝에 괘선만 남았다.
표제란은 칸의 경계가 정보라서, 접히는 자리를 우연에 맡길 수 없다.

도면번호·축척·개정은 표제란에 넣지 않고 **작도 영역 옆 기입란 머리(`SheetStamp`)** 에 둔다 —
도면이 식별정보를 적는 자리가 거기고, 값이 도착하기 전 비어 있던 우상단 사분면을 그것이 채운다.

### Callout Number (원번호)
도면과 값 표를 잇는 유일한 끈. 지시선 끝에 원을 그리고 그 안에 두 자리 번호(`01`~`07`)를 적는다.
지름은 글자 크기에서 뽑는다(`fontSize × 1.75`) — 18dp로 고정했더니 시스템 글자 1.3배에서 뒷자리가
잘려 "03"이 "0"으로 보였다. 지목되면(`highlighted`) 원이 채워지고 글자가 종이색으로 뒤집힌다.
경보면 원과 라벨이 함께 정정 적으로 바뀐다.

### Table (명세표)
머리글은 label-small + 가장 얇은 잉크, 그 아래를 **1dp 잉크 괘선**으로 닫는다(도면 표의 관례).
행 경계도 괘선이다 — 카드 간 여백으로 나누면 표가 아니라 목록이 된다.
행 전체가 편집 진입점이다. 편집 버튼을 따로 두면 행이 버튼 창고가 된다.

### Inline Banner
토스트를 쓰지 않는다 — 차량 화면에서 스쳐 지나가면 왜 실패했는지 놓친다.
도면의 주기(註記)는 색 면이 아니라 **두 줄 사이**에 놓인다. 위아래 2dp 괘선 사이에 메시지와 "닫기"뿐이다.
옅은 색 면으로 깔면 다른 경보와 유채색을 나눠 쓰게 되고, 그러면 The One Alert Rule이 무너진다.

### Progress
원형 스피너를 쓰지 않는다 — Material 색을 물고 들어와 팔레트를 깨뜨린다.
화면 맨 위 2dp 선이 왼쪽 밖에서 오른쪽 밖으로 한 번 훑고 반복한다(1200ms). 꺼져 있을 때도
자리를 유지해 레이아웃이 튀지 않는다. 애니메이션을 끈 기기에서는 훑지 않고 **가득 찬 선**으로 남는다.

### Signature Component: Model Y 평면 선도 (`CarPlan`)
이 앱에서 가장 중요한 판단이 여기 있다. 차의 상태를 목록이 아니라 **차의 모양**으로 말한다.

- **실물 비례 2.47:1**(4,751 × 1,921mm)을 그대로 쓴다. 도면이 실물 비례를 안 지키면 삽화다.
- **평면도는 위에서 내려다본 그림**이라 노즈가 화면 왼쪽이면 **차량 좌측(운전석)은 화면 아래**다.
  처음엔 운전석을 위에 뒀는데 차를 아는 사람은 3초 안에 알아챈다.
- 바퀴 4개(앞축 0.205 · 뒷축 0.805, 실제 휠베이스 비율)를 **파선**으로 그린다 — 펜더에 덮여 있으니
  숨은 선이고, 차체선에는 휠하우스 개구부를 짧은 직각 표시로 낸다. 바퀴가 없으면 이 그림은 알약으로 읽힌다.
- 타이어는 차체 **안쪽**에 있다. 윤거 1,635mm가 전폭 1,921mm보다 좁다 — 밖으로 빼면 오픈휠 경주차가 된다.
- 사이드미러 두 개가 앞뒤를 갈라 준다. 노즈 곡률만으로는 약했다. 바퀴보다 얕고 얇게 — 같은 크기면
  바퀴 여섯 개로 읽힌다.
- 윈드실드·리어글래스는 **곡선**이다. 직선으로 그으면 캐빈이 상자로 보인다.
- **열린 문**은 경첩을 축으로 45도 벌어진 판 + 끝단 직각 표시로 그리고, 원래 닫혀 있던 자리를 파선으로 남긴다.
  전부 정정 적이다.
- 부위 탭이 **주 조작**이다. 탭 판정은 그리기와 같은 정규 좌표를 쓰고, 작은 부위(시트)를 먼저 본다 —
  순서를 바꾸면 시트를 눌러도 차체가 잡힌다.
- 누르면 화면을 덮는 시트가 아니라 **기입란 자리에 상세도가 펼쳐진다.** 시트로 덮으면 도면이 안 보이고,
  그러면 "어디를 조작하는 중인지"가 사라진다. `BackHandler`가 상세도를 닫는다.
- 캐빈 해칭이 **앱에서 유일하게 움직이는 것**이다(2600ms 선형 반복). 공조가 실제로 도는 동안만 돌고,
  정상이면 화면은 완전히 정지한다. 시스템 "애니메이션 제거"를 켜면 무늬는 남고 흐름만 멈춘다.

### 잔존물 (사용하지 않음)
`StatusPill`, `StatusTile.kt`, `MetricTextStyle`, `TileValueStyleLarge`는 이전 세계(0.8.31)의 남은 코드이고
지금 어느 화면에도 렌더되지 않는다. 시스템의 일부가 아니다 — 새 화면에서 꺼내 쓰지 않는다.

## Do's and Don'ts

### Do:
- **Do** 층이 필요하면 **괘선 하나**를 긋는다(`TCard` = 위 1dp, 강조 2dp). 상자를 그리지 않는다.
- **Do** 계층을 색이 아니라 **선 굵기 3계층**(0.5 / 1 / 2dp)으로 만든다. 정보를 지고 있는 선은 최소 1dp다.
- **Do** 숫자·단위·부품번호·시각에는 항상 고정폭 + `tnum`을 쓴다.
- **Do** 상태를 바꿀 때 **색과 무늬를 함께** 바꾼다 — 윤곽선 / 해칭 / 채움 / 파선.
- **Do** 값을 아직 못 읽었으면 `--`나 "치수 미기입"으로 둔다. 추측해서 채우지 않는다.
- **Do** 새 아이콘이 필요하면 `DraftMark`에 24dp 격자·1.5dp 단일 굵기·사각 끝단·채움 없음으로 추가한다.
- **Do** 새 터치 타깃은 **48dp 이상**으로 잡는다.
- **Do** 간격은 `Space` 토큰(4·8·16·24·32·48)에서만 꺼낸다.
- **Do** UI를 바꿨으면 `WideScreenshotTest`(실기기 960×600dp)로 눈으로 확인한다.
  기본 `ScreenshotTest`(PIXEL_C)는 실기기보다 가로 320dp·세로 300dp 넓어 접힘과 잘림을 못 잡는다.
- **Do** 글자 배율 1.3배에서 잘리는지 본다. 원·줄 높이는 `fontScale`에 비례시키고,
  기입 치수는 칸 폭에 닿는 배율(1.3)까지만 따라 키운다 — 나누면 접근성 의도가 거꾸로 된다.

### Don't:
- **Don't** 그림자·글로우·그라데이션을 쓰지 않는다. 하나도 없다.
- **Don't** 0dp가 아닌 모서리를 만들지 않는다. 999dp는 실제로 원인 것에만.
- **Don't** 정정 적·제도 청 밖의 유채색을 새로 만들지 않는다. 정상은 무채색이다.
- **Don't** 유채색을 한 화면에 두 군데 동시에 띄우지 않는다 — 봐야 할 곳이 하나여야 한다.
- **Don't** 정상 상태에 초록을 주지 않는다. 도면에서 "이상 없음"은 표시가 없다는 뜻이다.
- **Don't** Material 아이콘·Material Switch·이모지를 쓰지 않는다. 도면 기호와 채운/빈 사각으로 한다.
- **Don't** 원형 스피너와 토스트를 쓰지 않는다. 훑는 2dp 선과 화면 안 배너로 한다.
- **Don't** 시트 1·2 본문을 좌우 2단으로 나누지 않는다. 2단은 설정 시트만의 예외다.
- **Don't** 시트 1에 스크롤을 넣지 않는다. 차에 고정된 화면에서 스크롤은 못 본 정보를 만든다.
- **Don't** 화면에 큰 제목을 두지 않는다. 어느 시트인지는 좌측 목차가 이미 말한다.
- **Don't** 칸을 여백으로 나누지 않는다. 괘선으로 나눈다.
- **Don't** 0.8.31의 세계를 되살리지 않는다 — 무채색 회색 팔레트, 4dp 모서리, 1dp 테두리 카드,
  검정 액센트, Material Icons Rounded, Material Switch, 히어로 타일. 전부 안티레퍼런스다.
- **Don't** 표의 열 비율을 머리글과 본문에서 따로 쓰지 않는다. 상수 하나를 나눠 갖는다.
- **Don't** 반복 애니메이션을 정상 상태에 넣지 않는다. 움직임은 "차가 지금 일하는 중"이라는 뜻이다.

---

## 결정된 것

**UX 문안의 두 어조는 그대로 둔다** (2026-08-21, 사용자 결정). 시트 1은 제도체 한국어로 말한다(`치수 미기입`, `차량 미등록`, `모두 닫힘`).
시트 2·3은 대화체로 말한다(`세차·정비 중에는 꺼두세요`, `조건이 맞으면 여기에 기록이 쌓여요`,
`앱에서만 지워져요`). 어느 쪽으로 통일할지, 혹은 "계측값은 제도체 · 설명은 대화체"로 역할을 갈라
그대로 둔다. **역할이 다르기 때문이다** — 시트 1은 계기가 값을 기입하는 자리이고,
시트 2·3은 사람에게 설명하는 자리다. 한쪽으로 통일하면 계기가 수다스러워지거나
설명이 차가워진다. 마감 리뷰어가 "한 도면집에 저자가 두 명"이라고 지적한 건이지만,
이 앱에서는 의도된 분리로 남긴다.
