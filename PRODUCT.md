# Product

<!-- impeccable:product-schema 1 -->

## Platform

android

## Users

주 사용자는 휴대폰으로 자기 테슬라(Model Y)의 매크로와 설정을 조작하는 앱 소유자다.
차내 거치 태블릿도 지원하지만 UI의 기본 기준은 휴대폰이다.
사용 장면은 셋이다:

1. **탑승 직전·직후** — 차에 다가가거나 문을 열고 앉는 몇 초. 손에 짐이 있거나 시동을 걸며 곁눈으로 본다.
2. **주차된 차 안에서 대기** — 차박·충전 대기·잠깐 쉬는 시간. 앉아서 조작한다.
3. **주행 중** — 길안내를 넘긴 내비가 화면을 덮고 있다. 이때는 **조작이 아니라 표시**만 한다 —
   속도가 창 하나로 떠 있고, 손은 운전대에 있다. 여기서 뭘 누르게 만들면 안 된다.

1·2 사이 대부분의 시간에는 **아무도 안 만진다.** 매크로가 알아서 하기 때문이다.
휴대폰은 필요할 때 열어 확인하고, 거치 기기는 차 안에서 상태를 표시한다.
낮의 직사광과 밤의 어두운 환경을 함께 고려한다.

## Product Purpose

클라우드를 거치지 않고 BLE로 테슬라를 직접 제어한다. 공조·시트·잠금·충전·문을
직접 누르거나, 조건(시각·위치·온도·탑승)을 걸어 매크로로 자동 실행한다.
성공은 "조작이 편하다"가 아니라 **"내가 손대기 전에 이미 되어 있다"** 다.

## Positioning

공식 앱과 서드파티 앱은 전부 테슬라 클라우드를 경유한다 — 계정·토큰·네트워크가 필요하고
응답이 수 초 걸리며 서버가 죽으면 같이 죽는다.
이 앱은 **차량 옆 5m 안에서 BLE로 직접** 말한다. 차 옆에 있는 한 인터넷이 끊겨도 동작한다.

**클라우드를 금지하지는 않는다.** BLE가 정본이고, 네트워크는 BLE로 못 하는 일을
보태는 데 쓴다 (앱 업데이트가 이미 그렇게 쓰고 있다). 다만 인터넷이 있어야만 되는
기능을 핵심 조작 경로에 놓지 않는다 — 차 옆에서 공조를 켜는 데 서버가 끼면 안 된다.

## Operating Context

- **기기**: 휴대폰이 주 사용 기기다. 차량에는 ALLDOCUBE iPlay 60 mini Pro, 8.4" 1920×1200 (약 960×600dp)를 두고,
  동일한 앱으로 휴대폰과 거치 기기를 지원한다. 처음 설치하면 휴대 모드이며, 차량에 계속 두는 기기만
  종류와 상관없이 거치 모드로 바꿔 설치본마다 연결 정책을 분리한다.
  값싼 패널이라 순검정에서 잔상이 남고 명암 재현이 좋지 않다.
- **거치**: 차 안에 계속 두는 기기에서 선택하는 보조 사용 형태다.
- **입력**: 휴대폰에서 한 손으로 읽고 누르며, 거치 기기는 정차 중 손을 뻗어 조작한다.
- **조명**: 낮과 밤이 완전히 다른 환경. 시계(07~19시)로 낮/밤 팔레트를 스스로 뒤집는다.
- **연결**: 저장된 MAC 직행 + autoConnect. 스캔은 이 기기에서 광고를 못 받는다.
  BLE 요청은 직렬로만 — VCSEC 응답에 request_uuid가 없어 병렬이면 응답이 뒤바뀐다.
  휴대 모드는 앱 화면·직접 명령에만 연결한다. 거치 모드도 빈 차가 확인되면 USB 전원이 남아 있어도
  앱·명령·매크로가 모두 쉬는 즉시 인증 BLE를 끊는다.
- **배포**: GitHub Release + 앱 자체 업데이트(PackageInstaller). 스토어를 거치지 않는다.

## Capabilities and Constraints

**할 수 있는 것**
- 공조 켜기/끄기·목표 온도, 앞좌석 통풍/열선 3단, 잠금/해제, 충전 한도·전류·스텔스 충전 1회,
  창문 환기/닫기, 트렁크 열기/닫기, 프렁크 열기
- 매크로: 조건(시각·요일·위치·실내 온도·탑승·배터리·**오늘 예보**)과 동작(위 명령 + 대기)을 엮어 자동 실행
- 차량 상태 폴링(배터리·주행거리·온도·잠금·문 열림·**타이어 공기압·차량 위치·차량 SW 업데이트**)
- 매크로·설정을 파일로 내보내고 되돌리기 (차량 식별자와 키는 담기지 않는다)
- 길안내를 내비 앱 4종(네이버·카카오내비·티맵·구글)에 넘기기
- 주행 중 속도 표시 — 제어 화면 기입란과, 다른 앱 위에 뜨는 창(설정으로 전환)
- 과속·구간단속·보호구역 안내 — 화면(제어 화면 기입란 + 속도 창)과 소리 양쪽.
  소리는 끌 수 있고 음량이 3단이며, 내비 음성을 끊지 않고 잠깐 낮춘다

**제약**
- 창문·트렁크 등 일부 명령은 **차가 거부할 수 있다** (주차 상태·차량 정책). 앱이 막는 게 아니다.
- 상태 읽기는 카테고리별로 따로 도착한다 — 어떤 값은 있고 어떤 값은 아직 `--`인 순간이 정상이다.
  여러 상태를 한 요청에 묶어 읽되, 차가 "응답이 너무 크다"고 하면 하나씩으로 물러선다.
- 안전 안내의 제한속도는 **다가오는 단속 카메라의 것뿐**이다 — 경로 없이 도는 모드에는
  "지금 달리는 도로의 제한속도"라는 값이 오지 않는다. 카메라가 없는 구간의 과속은 알 수 없다.
- 위성을 못 잡으면 "안내할 게 없음"이 아니라 **"위치 없음"이라고 적는다.** 못 하는 것을
  침묵으로 감추면 사용자가 없는 안내를 믿는다.
- 연결이 끊기면 사용 중에는 자동으로 다시 붙고, 세션이 어긋나면 다시 세운다. 잠든 차는 게이트웨이가
  깨워서 다시 보낸다 — 단 멱등 명령만. 트렁크·경적은 응답이 없어도 한 번으로 끝낸다.
- 휴대폰 키 간섭 방지는 거치 모드에서 기본 켜짐이다. 스텔스 충전 1회를 켠 동안만 거치 기기 연결을
  유지하고, 충전 완료 또는 사용자가 끄면 원래 전류를 복구한 뒤 기존 보호 정책으로 자동 복귀한다.
- 휴대 모드는 보호 설정과 무관하게 백그라운드 자동 연결·자동 매크로를 하지 않는다.
  단, 탑승 안심운전 자동 시작은 사용 모드가 아니라 해당 옵션을 따른다.
  강제 종료 대신 설정이나 감시 알림의 `차량 연결 끊기`로 다음 사용까지 즉시 일시정지할 수 있다.
- 등록 = VIN 저장(isPaired) + 카드키 핸드셰이크(isEnrolled). 둘은 별개 상태다.
- 프레임워크를 선제 도입하지 않는다 (Hilt·Room 의도적 미채택). 모듈 2개: `:app` + `:tesla-ble`.

## Brand Commitments

- 이름은 **Smart Tesla**. 전용 서체 없이 시스템 서체를 사용한다.
- 테슬라의 상표·서체·UI를 흉내내지 않는다. 이 앱은 테슬라의 것이 아니다.
- **사용자 실 VIN을 코드·테스트·문서·로그 샘플에 절대 넣지 않는다.** 더미는 `5YJS0000000000000`.
- 진단 로그에 좌표 원문을 남기지 않는다 — 공유되는 통로다.
- 한국어 UI. 한 문장 = 한 줄.

## Evidence on Hand

- 실차 1대(Model Y)와 실기기 1대. 모든 판단의 근거는 이 조합에서 실측된다.
- 휴대폰 낮/밤과 태블릿 기본·글자 확대 Paparazzi 스냅샷 (`app/src/test/snapshots/images/`) — 현행 화면의 시각적 정본.
- 에뮬레이터 AVD `tesla_tablet` 존재 — 실기기 크기 검증 가능.
- 프로토콜 정답 벡터(ProtocolVectorTest) — 불변. 안 맞으면 코드가 틀린 것.
- **없는 것**: 사용자 리서치, 다른 사용자, 사용 통계, 로고·일러스트·사진 자산.
  이것들을 있는 것처럼 꾸미지 않는다.

## Product Principles

1. **안 만지고 알아채는 것이 먼저다.** 조작 편의보다 "흘깃 봐서 아는 것"이 위다.
2. **정상은 조용해야 한다.** 선택과 주요 동작에는 강조색을 쓰고, 상태 변화는 글자와 함께 구별한다.
   상시 켜진 화면에서 상시 켜진 강조는 강조가 아니다.
3. **못 읽은 값은 없는 값이다.** 추측해서 채우지 않는다 — `--`가 정직한 표시다.
4. **한 손으로, 흔들리는 차에서.** 타깃은 크게, 위치는 고정. 긴 내용은 스크롤하고 주요 저장 동작은 쉽게 접근하도록 둔다.
5. **휴대폰에서 먼저 읽혀야 한다.** 매크로와 설정을 한 열로 정리하고 거치 화면으로 확장한다.

## Accessibility & Inclusion

- 터치 타깃 최소 48dp (정차 중 조작 · 흔들림 · 장갑).
- 직사광과 야간 암순응을 모두 통과해야 한다 — 낮/밤 두 팔레트가 각각 대비를 만족해야 한다.
- 시스템 글자 크기 확대(1.3배)에서 라벨이 잘리지 않아야 한다.
- 시스템 "애니메이션 제거" 설정을 존중한다 (`ReducedMotion`).
- 색만으로 상태를 전달하지 않는다 — 경보는 색 + 면 + 글자.
